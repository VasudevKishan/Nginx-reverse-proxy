version: '3'
services:
  nginxReverseProxy:
    image: nginx
    ports:
      - '8080:8080'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - bookmark-network

networks:
  bookmark-network:
    external: true
